import Table from "../containers/table/Table";
import React from "react";
import { render } from "react-testing-library";

test("should have the `button` element", () => {
  expect(true).toBeTruthy();
});
